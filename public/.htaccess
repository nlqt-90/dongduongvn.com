# Cấu hình Content-Type cho YAML files
<IfModule mod_mime.c>
    AddType text/yaml .yml
    AddType text/yaml .yaml
</IfModule>

# Đảm bảo config.yml được serve với đúng Content-Type
<FilesMatch "\.(yml|yaml)$">
    Header set Content-Type "text/yaml; charset=utf-8"
</FilesMatch>

